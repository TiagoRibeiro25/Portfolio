export type AboutOptions = "whoAmI" | "skills" | "resume";

export type ProjectId = `${string}-${string}-${string}-${string}-${string}`;

export type MainButtonName = "Play" | "Quit";

export type HomeLinkName = "About" | "Projects" | "Contact" | "Settings";

export type HomeLinkLinks = "/about" | "/projects" | "/contact" | "/settings";
